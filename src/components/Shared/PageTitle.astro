---
const { title, description } = Astro.props;
---

<div class="bg-white px-6 pb-24 pt-36 sm:pb-20 sm:pt-40 lg:px-8">
	<div class="mx-auto max-w-2xl text-center">
		<h2 class="text-4xl font-bold uppercase tracking-tight text-gray-900 sm:text-6xl">{title}</h2>
		{description ? <p class="mt-6 text-lg leading-8 text-gray-600">{description}</p> : ""}
	</div>
</div>
